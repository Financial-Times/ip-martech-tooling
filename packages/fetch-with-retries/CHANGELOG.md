# Changelog

## 1.0.0 (2026-06-01)


### Features

* add the `fetch-with-retries` package for native Node `fetch`
* add configurable retry attempts, backoff strategy, and structured retry logging
* add opt-in retry support for `POST` and `PATCH` requests


### Documentation Changes

* document package usage, retry policy, logging events, and TypeScript support
