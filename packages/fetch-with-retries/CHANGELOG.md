# Changelog

## 1.0.0 (2026-06-03)

### Features

- adds the `fetch-with-retries` package for native Node `fetch`
- adds configurable retry attempts, backoff strategy
- adds opt-in retry support for `POST` and `PATCH` requests

### Documentation Changes

- documents package usage, retry policy, and logging events
